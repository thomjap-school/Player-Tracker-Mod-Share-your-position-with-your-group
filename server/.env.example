# Copie ce fichier en `.env` (non versionné) et remplis tes valeurs.
#   cp .env.example .env

# Domaine ngrok FIXE (réclamé sur https://dashboard.ngrok.com/domains).
# Utilisé par ./start-ngrok.sh.
NGROK_DOMAIN=ton-domaine.ngrok-free.dev

# Port local du serveur relais (optionnel, défaut 8000).
PORT=8000
